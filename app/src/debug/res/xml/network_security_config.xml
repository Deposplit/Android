<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow plain HTTP to a relay on the development machine's port 9000: an emulator reaches it
         through the host alias 10.0.2.2, a phone through `adb reverse tcp:9000 tcp:9000` as localhost -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
