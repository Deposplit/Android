# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Resolve toolchains only from the JDK that gradlew was launched with, i.e. the one on
# JAVA_HOME, rather than from whatever Gradle finds lying around.
#
# Inert today: the build uses compileOptions rather than jvmToolchain(N), and nothing pins
# the daemon JVM either, so no toolchain resolution is requested at all. It is kept as a
# cheap guard should anyone add one — auto-detection scans VS Code extension directories,
# and the redhat.java extension's bundled Java 21 image has javac but no jlink, which AGP's
# JdkImageTransform needs.
#
# This property neither caused nor cured the jlink failure this project hit; a
# gradle/gradle-daemon-jvm.properties pin did. CLAUDE.md has that story.
org.gradle.java.installations.auto-detect=false
